task npmStart(group: 'npm', type: Exec) {
    commandLine 'npm', 'start'
}

task npxPrettierWrite(group: 'npm', type: Exec) {
    commandLine 'npx', 'prettier', '--write', '.'
}

task npxPrettierCheck(group: 'npm', type: Exec) {
    commandLine 'npx', 'prettier', '--check', '.'
}
